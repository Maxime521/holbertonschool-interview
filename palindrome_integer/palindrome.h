#ifndef PALINDROME_H_H
#define PALINDROME_H_H

#include <stddef.h>

/**
 * is_palindrome - checks if a number is a palindrome
 * @n: the number to check
 *
 * Return: 1 if the number is a palindrome, 0 otherwise
 */

int is_palindrome(unsigned long n);







#endif
